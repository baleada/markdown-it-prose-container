import test from 'ava'
import containerMetadataToProps from '../src/util/containerMetadataToProps'

test.beforeEach(t => {
  
})

test(`transforms metadata to ProseCodeblock's required props`, t => {
  
})

test(`transforms metadata to ProseHeading's required props`, t => {
  
})

test(`transforms metadata to ProseTable's required props`, t => {
  
})

test(`transforms metadata to ProseList's required props`, t => {
  
})

// Non-required props are handled by infoToProps, which is tested separately
